#spring:
#  datasource:
#    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:testdb}
#    driver-class-name: org.postgresql.Driver
#    username: ${DB_USERNAME:postgres}
#    password: ${DB_PASSWORD:root}
#  jpa:
#    database-platform: org.hibernate.dialect.PostgreSQLDialect
#    hibernate:
#      ddl-auto: ${JPA_DDL_AUTO:none}
#    show-sql: ${JPA_SHOW_SQL:false}


spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:${MYSQL_PORT:3306}/${MYSQL_DB:jwtdb}?createDatabaseIfNotExist=true
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:none}
    show-sql: ${JPA_SHOW_SQL:false}

redis:
  hostname: localhost
  port: 6380
  auth-token: ""
  database: 0

app:
  redis:
    enable: false

whatsapp:
  infobip:
    url: ${INFOBIP_BASE_URL:https://gg83ee.api.infobip.com}
    sms:
      delay: 1500
  infobipApiKey: ${INFOBIP_API_KEY:e60a30e7ca0cfda3e69d}
  app-base-url: https://phyllotactical-valeri-unelusively.ngrok-free.dev
  settings:
    countries:
      india:
        phone-number: "447860088970"
        start-keywords: "hi,hello,hey,HELLO"
        defaultTerminateMessage: ""
        scenarioKey: ${INFOBIP_SCENARIO_KEY:DA05E3E276BFE}
